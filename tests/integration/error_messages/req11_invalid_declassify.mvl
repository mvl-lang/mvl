// Expected error: `declassify()` requires `Secret<T>`, found `Int`
// Triggers: CheckError::InvalidDeclassify (req11)

fn main() -> Unit ! Console {
    let x: Int = 42;
    let y: Int = declassify(x);
    println("{}", y);
}
