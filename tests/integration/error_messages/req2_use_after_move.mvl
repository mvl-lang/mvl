// Expected error: use of moved value `s`
// Triggers: CheckError::UseAfterMove (req2)
// Uses consume() builtin to explicitly transfer ownership, then reads the original.

fn main() -> Unit ! Console {
    let s: String = "hello";
    let _a: String = consume(s);
    let _b: String = s;
}
