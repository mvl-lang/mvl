[workspace]
members = [".", "mvl_runtime"]

[package]
name = "mvl"
version = "0.51.0"
edition = "2021"
description = "MVL — Minimum Verification Language compiler"
license = "MIT"

[dependencies]
ureq = "2"
tempfile = "3"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
