[workspace]
members = [".", "mvl_runtime"]

[package]
name = "mvl"
version = "0.9.0"
edition = "2021"
description = "MVL — Minimum Verification Language compiler"
license = "MIT"

[dependencies]

[dev-dependencies]
