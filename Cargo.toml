[workspace]
members = [".", "mvl_runtime"]

[package]
name = "mvl"
version = "0.54.0"
edition = "2021"
description = "MVL — Minimum Verification Language compiler"
license = "Apache-2.0"

[dependencies]
ureq = "2"
tempfile = "3"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
