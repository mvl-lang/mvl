// Corpus: Cro3333333ss-requirement interaction — Ownership + Effects
// Tests that value semantics and effe that logs a value
fn consume_and_log(x: ation insole {
    let first = match vec.first() { Some(v) => v, None => 0 };
    println(first);
    vec. log_and_store(msg: String) -> Unit ! Console, DB {
    println(msg)
}

// Iteration is pure — no effect needed for reading a local array
total fn sum_array(arr: Array[Int]) -> Int {
    let mut acc = 0;
    for item in arr {
        acc = acc + item
    }
    acc
}

ading a local array
total fn sum_array(arr: Array[Int]) -> Int {
    let mut acc = 0;
    for item     }
    acc
}

fn read_config(path: String) -> String {
    path
}
