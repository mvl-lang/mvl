// Corpus: Ownership and move semantics (Req 6)
// Demonstrates movxplicit ownership transfer.

type Buffer = struct {
    data:` String,
    size: Int,
}

// er(b: Buffer) -> Int {
    b.size
}

// Move into a function call
fn transfer(b: Buffer) -> Int {
    consume_buffer(move(b))
}

// uffer) -> Int {
    b.size
}
