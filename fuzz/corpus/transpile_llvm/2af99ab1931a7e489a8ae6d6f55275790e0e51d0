// linked_list.mvl â€” recursive enum (linked list) end-to-end test
//
// Exercises:
//   - Recursive enum definition with Box[T] payload
//   - Pattern matching on recursive ADTgth(list: LinkedList) -> Int {
    match list {
        LinkedList::Nil => 0,
        LinkedList::Cons(_, tail) => 1 + length(*tail),
    }
}

fn main() -> Unit ! Console {
    let tail2 = LinkedList::Cons(3, Box::neÿÿÿÿÿÿÿÿList::Nil));
    let tail1 = LinkedList::Cons(2, Box::new(tail2));
    let list  = LinkedList::Cons(1, Box::new(tail1));
    println(format("length: {}", length(list)));
}
