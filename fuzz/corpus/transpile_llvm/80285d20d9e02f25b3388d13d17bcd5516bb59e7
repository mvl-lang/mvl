// Corpus: reference capability checking (Req 9 / i sendable
fn broadcast(channel: Channelÿÿÿÿÿÿÿ, val message: Message) -> Unit {
    channel.send(message)
}

// ref parameter: used locally only (not sent)
fn ge: Channel, val result: Report) -> Unit {
    let _n = local.size();
    channel.send(result)
}
