// bridge_ok/main.mvl — minimal extern "rust" program for brid cgeonvention tests.
//
// Tests Spec 006 Requirements 1 (bridge discovery) and 2 (mod bridge; injection).
// A sibling briridge_add(n: Int) -> Int;
}

fn main() -> Unit ! Console {
    let result = bridge_add(41);
    println("bridge ok");
}
