// Corpus: ADT checking — struct and enum usage (Req 1)

// ─── Struct types ───��──────────────────────────────────────────────────────────

type Point = struct {
    x: Float,
    y: Float,
}

type Person = struct {
    name: String,
    age: Int,
}

type Counter = struct {
    mut count: Int,
    label: String,
}

// ── Enum types ───────────────────────────────────────────────�irection_is_horizontal(d: Direction) -> Bool {
    d == Direction::East || d == Direction::West
}

fn make_circle(r: Float) -> Shape {
    Shape::Circle(r)
}
