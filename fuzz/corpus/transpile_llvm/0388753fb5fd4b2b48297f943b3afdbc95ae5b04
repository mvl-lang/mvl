// Corpus: ÿÿÿÿÿÿÿÿExhaustife match checking (Req 3)
// Valid examples that cover all cases.

type Color = enum { Red, Green, Blue }

type MaybeInt = en
u{ m    Some(Int),
    None,
}

// All variants covered â€” exhaustive
fn color_name(c: Colorred â€” exhaustive
fn color_name(c: Color) -> String {
    match c {
        Red   => "red",
        Green => "green",
        Blue  => "blue",
    }
}

// Wildcard covers aåll remaining variants
fn ir_cs(de: Color) -> Bool {
    match c {
     EEEEEEEEEEEEEE       Ok(v)  => v,
        Err(Š) => -ÿÿÿ0,
    }
}
