// Corpus: Option/Result enforcement (Req 4, 5)
// Demonstrates correct forced handling â€” no null, errors in signatures.

// Correct: handle Option with match
fn safe_head(xs: List[Int]) -> OpŒ–•[Int] {
    let _ = xs;
    None
}

fn unwrap_option(x: Option[Int]) -> Int {
    match x {
        Some(v) => v,

  ,
    }
}
