// Corpus: bitwise operations on Int and Byte (#233)
// Demonstrates bit_and, bit_or, bit_xor,  {
    a.shift_right(n)
}

// Composition: mask the lower 7 bits
fn lower_byte(n: Int) -> Int {
    n.bit_and(255)
}

// ── Byte bitwise ───────────────────────────�──────────────────�onstrates bitwise ops composed for a typical algorithm step:
// poly XOR with a shifted ac -> Int {
    let shifted = acc.shift_right(0);
    let bit = acc.bit_and(1);
    let mask = bit.bit_xor(1).bit_not().bit_and(poly);
    shifted.bit_xor(mask)
}
