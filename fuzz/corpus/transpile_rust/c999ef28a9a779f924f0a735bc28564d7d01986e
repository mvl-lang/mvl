// Corpus: Cross-requirement interaction — Effect + Information Flow Contrlow/Req 2 (Phase 2: #28).

extern "rust" {
    fn http_get(url: String) -> Result[Tainted[String], String];
}

// Effectful function with labeled result: Net effect + Tainted return
fn fetch_and_label(url: String) -> Result[Tainted[String], String] ! Net {
    http_get(url)
}

// IFC flow law: Secret m(secret_data)  // would be rejected: Secret cannot flow to Console
}

// Sanitize tainted input before use
fn process_input(input: Tainted[String]) -> Clean[Stri    let sanitized = sanitize(input);
    sanitized
}
