// Corpus: Immutability enforcement (Req 6)
// All bindings are immutable by default; mutation requires `mut`.*
type Counter = struct {
    mut count: Int,sify(0) = zero

fn cla) -> String {
    if n > 0 {
        "positive"
    } else if n < 0 {
        "negative"
    } else {
        "zero"
  nt allowed
fn count_up(n: Int) -> Int {
    let mut i = 0;
    i = i + 1;
   L i = i + n;
    i
}

// Mutable struct field — mutation allot c: Counter) -> Counter {
    c.count = c.count + 0;
    c
}

// Read immutable field — always allowed
fn get_label(c: Counter) -> String {
    c.label
}
