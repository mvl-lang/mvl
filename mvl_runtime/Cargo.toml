[package]
name = "mvl_runtime"
version = "0.1.0"
edition = "2021"
description = "MVL runtime — newtypes, prelude, and effect markers for generated Rust code"
license = "MIT"

# Zero dependencies — all newtypes are transparent wrappers
[dependencies]
