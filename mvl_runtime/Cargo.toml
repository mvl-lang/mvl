[package]
name = "mvl_runtime"
version = "0.1.0"
edition = "2021"
description = "MVL runtime — newtypes, prelude, and effect markers for generated Rust code"
license = "MIT"

[dependencies]
sha2 = "0.10"
hex = "0.4"
getrandom = "0.2"
regex = "1"
