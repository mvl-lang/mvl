/// Arithmetic extension methods on Point.
use point::Point;

pub fn Point::sum(self) -> Int {
    self.x_val() + self.y_val()
}

pub fn Point::product(self) -> Int {
    self.x_val() * self.y_val()
}
