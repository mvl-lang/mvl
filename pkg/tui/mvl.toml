[package]
name            = "tui"
version         = "1.0.0"
license         = "Apache-2.0"
requires-mvl    = ">=0.90.0"
extern-rationale = "wraps ANSI terminal control; bridge uses POSIX libc (always linked) — no extra crates required"

# [native] — Rust crates used in bridge.rs
# None for Phase 7: bridge uses std-only + POSIX stty/ANSI escape codes.
# Phase 4: crossterm = "0.28", ratatui = "0.26" (widget layer)
