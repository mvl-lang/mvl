[package]
name = "mvl_runtime_rust"
version = "1.0.3"
edition = "2021"
description = "MVL runtime — newtypes, prelude, and effect markers for generated Rust code"
license = "Apache-2.0"

[dependencies]
mvl_runtime_core = { path = "../core" }
sha2 = "0.10"
hex = "0.4"
getrandom = "0.2"
regex = "1"
